#include<stdio.h>
#include<math.h>
int main(){
	int i,j,a[11],len,diff,max=0;
	len=sizeof(a)/sizeof(a[0]);
	printf("Enter the elements of array: ");
	for(i=0;i<len;i++){
		scanf("%d",&a[i]);
	}
		for(i=0;i<len;i++){
			if(a[i]>0){
				
			}
		}	
	}
	
